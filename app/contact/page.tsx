'use client'; import {useState} from "react";
export default function Contact(){const [message,setMessage]=useState("");const [token,setToken]=useState("");
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),organization:f.get("organization"),subject:f.get("subject"),message:f.get("message"),website:f.get("website"),recaptchaToken:token})});setMessage(r.ok?"Message received.":"Submission failed.");}
return <main className="mx-auto max-w-3xl px-6 py-24"><h1 className="text-6xl font-black">Contact</h1><p className="mt-4 text-white/60">Production form includes server-side validation, honeypot and reCAPTCHA verification.</p><form onSubmit={submit} className="mt-10 grid gap-4"><input name="name" required placeholder="Name" className="rounded-xl bg-white/5 p-4"/><input name="email" required type="email" placeholder="Email" className="rounded-xl bg-white/5 p-4"/><input name="organization" placeholder="Organization" className="rounded-xl bg-white/5 p-4"/><input name="subject" required placeholder="Subject" className="rounded-xl bg-white/5 p-4"/><textarea name="message" required rows={7} placeholder="Message" className="rounded-xl bg-white/5 p-4"/><input name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" className="hidden"/><div className="rounded-xl border border-white/10 p-4 text-sm text-white/50">reCAPTCHA site key must be configured in production. Replace this block with the official reCAPTCHA widget/site integration.</div><input required placeholder="reCAPTCHA token" value={token} onChange={e=>setToken(e.target.value)} className="rounded-xl bg-white/5 p-4"/><button className="rounded-xl bg-white p-4 font-bold text-black">Send</button></form>{message&&<p className="mt-5">{message}</p>}</main>}
