import {NextResponse} from "next/server"; import {requireAdmin} from "@/lib/auth"; import {db} from "@/lib/db"; import {z} from "zod";
const schema=z.object({title:z.string().min(2).max(200),slug:z.string().regex(/^[a-z0-9-]+$/),excerpt:z.string().max(500).optional(),content:z.string().min(1),category:z.string().max(100).optional(),status:z.enum(["DRAFT","REVIEW","APPROVED","SCHEDULED","PUBLISHED","ARCHIVED"]).default("DRAFT")});
export async function GET(){const u=await requireAdmin();if(!u)return NextResponse.json({message:"Unauthorized"},{status:401});return NextResponse.json({success:true,data:await db.post.findMany({orderBy:{updatedAt:"desc"}})});}
export async function POST(req:Request){const u=await requireAdmin();if(!u)return NextResponse.json({message:"Unauthorized"},{status:401});const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({message:"Invalid data"},{status:400});const post=await db.post.create({data:p.data});return NextResponse.json({success:true,data:post},{status:201});}
